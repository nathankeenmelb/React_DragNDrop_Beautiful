//Draggable
const draggableSnapshot = {
  isDragging: true,
  draggingOver: "column-1" //the id of the droppable that the current dragged item is over, otherwise null
};

//Droppable
const droppableSnapshot = {
  isDraggingOver: true,
  draggingOverWith: "task-1"
};
